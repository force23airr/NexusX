{
  "name": "@nexusx/auction-engine",
  "version": "1.0.0",
  "private": true,
  "description": "NexusX auction engine — dynamic pricing, demand tracking, quality scoring",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@nexusx/database": "*",
    "@nexusx/types": "*",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
