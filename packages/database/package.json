{
  "name": "@nexusx/database",
  "version": "1.0.0",
  "private": true,
  "description": "NexusX shared database package — Prisma client, helpers, seeds, and migrations.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "ts-node scripts/migrate.ts",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "seed": "ts-node prisma/seeds/seed.ts",
    "reset": "ts-node scripts/reset.ts",
    "studio": "prisma studio",
    "format": "prisma format",
    "validate": "prisma validate",
    "db:push": "prisma db push",
    "db:embed": "ts-node scripts/embed-listings.ts",
    "db:embed:force": "ts-node scripts/embed-listings.ts --force",
    "db:embed:synthetic": "ts-node scripts/embed-listings.ts --synthetic-only",
    "db:embed:reindex": "ts-node scripts/embed-listings.ts --force --skip-synthetic",
    "postinstall": "prisma generate"
  },
  "prisma": {
    "seed": "ts-node prisma/seeds/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
